# JSON 入门

Python 字典会变成 JSON 对象：

```python
body = {"Argument": {"TYPE": "Pushover"}}
```

列表会变成 JSON 数组：

```python
"NODE": [1, 2]
```

字符串要加引号，数字和布尔值不要加引号：

```python
{"NAME": "C50", "ELAST": 0, "bELAST": False}
```
